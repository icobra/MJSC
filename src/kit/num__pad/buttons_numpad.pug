mixin numpad(items)
    form(class = "numpad")
        //На вход подаем список из {date:значение title:значение}
        each item in items
            button(id= item.title value= item.date)= item.title