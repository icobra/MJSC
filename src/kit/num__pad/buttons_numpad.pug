mixin numpad(items)
    form(class = "numpad")
        //На вход подаем список из {date:значение title:значение}
        each item in items
            button(id= item.date value= item.title)= item.title